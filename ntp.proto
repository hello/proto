package hello;

option java_package = "com.hello.suripu.api.ntp";

message NTPDataPacket {
    optional int64 reference_ts = 1;
    optional int64 origin_ts = 2;
    optional int64 receive_ts = 3;
    optional int64 transmit_ts = 4;
}